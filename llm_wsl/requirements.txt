torch>=2.1.0
deepspeed>=0.14.4
transformers>=4.37.0
datasets>=2.16.0
tqdm>=4.66.0
numpy>=1.24.0
matplotlib>=3.7.0
requests>=2.31.0
ninja>=1.11.1
pybind11>=2.11.0
accelerate>=0.26.0
sentencepiece>=0.1.99
protobuf>=4.25.0
mpi4py>=4.0.0
tiktoken>=0.5.1
safetensors>=0.4.0

# WSL Build Requirements Note:
# 1. Ensure CUDA Toolkit (nvcc) is installed: `sudo apt install nvidia-cuda-toolkit` or via conda
# 2. For C++ extensions, verify GCC/G++ version compatibility
# 3. For mpi4py: `sudo apt install libopenmpi-dev`
